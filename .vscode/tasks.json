{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "c:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "-I${workspaceFolder}/include",  // Include path for headers
                "-L${workspaceFolder}/lib",      // Linker path for libraries
                "${file}",                       // Source file to compile
                "-o", "${fileDirname}/${fileBasenameNoExtension}.exe",  // Output executable
                "-lglfw3dll",
                "${workspaceFolder}/src/glad.c",
            ],
            "options": {
                "cwd": "${workspaceFolder}"      // Set current working directory
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
